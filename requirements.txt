opencv-python>=4.8
# mediapipe 0.10.30+ removed the legacy Solutions API this app uses
# (0.10.21 is the last release with mp.solutions; needs 64-bit Python 3.9-3.12)
mediapipe>=0.10,<0.10.30
pyautogui>=0.9.54

# Windows (tray icon + brightness control)
pystray>=0.19 ; sys_platform == 'win32'
Pillow>=10.0 ; sys_platform == 'win32'
screen-brightness-control>=0.22 ; sys_platform == 'win32'

# macOS (menu bar)
rumps>=0.4 ; sys_platform == 'darwin'
